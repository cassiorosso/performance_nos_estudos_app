import 'package:flutter/material.dart';

var appBarWidget = AppBar(
      backgroundColor: Colors.grey[900],
      title: const Text('Performance nos estudos'),
      centerTitle: true,
      iconTheme: const IconThemeData(color: Colors.white)
    );